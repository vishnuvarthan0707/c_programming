#include<stdio.h>
int main ()
{  
    float a;
    printf("ENTER A NUMBER:");
    scanf("%f",&a);
    printf("%.2f is the given fractional number in 2 digit decimal format",a);
    return 0;
}